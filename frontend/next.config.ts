import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images:{
    domains:  ["images.unsplash.com", "plus.unsplash.com", "revision.codesupply.co"],
  },
  /* config options here */
};

export default nextConfig;